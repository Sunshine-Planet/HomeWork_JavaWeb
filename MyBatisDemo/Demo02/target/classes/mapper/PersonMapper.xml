<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.PersonMapper">
    <select id="findPersonById"
            parameterType="Integer"
            resultMap="IdCardWithPersonResult">
        select * from tb_person where id = #{id}
    </select>

    <select id="findPersonByIdG2"
            parameterType="Integer"
            resultMap="IdCardWithPersonResultG2">
        select *, p.id pid, c.id cid
        from tb_person p, tb_idcard c
        where p.card_id = c.id
        and p.id = #{id}
    </select>

    <resultMap id="IdCardWithPersonResult" type="Person">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex"/>
        <association property="card"
                     column="card_id"
                     select="mapper.IdCardMapper.findCodeById"
                     javaType="IdCard"/>
    </resultMap>

    <resultMap id="IdCardWithPersonResultG2" type="Person">
        <id property="id" column="pid"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex"/>
        <association property="card"
                     javaType="IdCard">
            <id property="id" column="cid"/>
            <result property="code" column="CODE"/>
        </association>
    </resultMap>
</mapper>
